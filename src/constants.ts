/**
 * Application constants - extracted magic numbers
 */

/** Duration to display toast messages before auto-dismiss (ms) */
export const MESSAGE_DISPLAY_DURATION_MS = 2000;

/** File watcher debounce interval (ms) */
export const FILE_WATCHER_DEBOUNCE_MS = 150;

/** Maximum number of changes to show in save preview modal */
export const SAVE_PREVIEW_MAX_ITEMS = 3;

/** Maximum length for clipboard display in footer */
export const TRUNCATE_CLIPBOARD = 30;

